# Changelog

كل تعديل بيتم على المشروع بيتسجل هنا، الأحدث فوق.

## 2026-09-03 (تحديث خامس)

### إلغاء رفض الصور اللي فيها أكتر من وش
- كان بيرفض أي صورة فيها أكتر من وش (multiple_faces_detected) - المشكلة:
  ده كان بيرفض صور فيها شخص واحد فعليًا + صورة معلقة على الحيطة، أو تاتو
  شكله وش، أو حتى صور تانية مش حقيقية بتتكشف غلط كـ"وش"
- رجعنا لنفس منطق الكود الأصلي: لو فيه أكتر من وش، ماخدش قرار رفض - بناخد
  الوش الأعلى ثقة (confidence) بس ونكمل بيه، بدل ما نرمي الصورة كلها
- شيل `multiple_faces_detected.json` نهائيًا من كل حاجة (checkpoints،
  الرفع، التقارير)
- اتأكد من السلوك الجديد باختبار فيه وشين مختلفين في الثقة - الوش
  الأعلى ثقة هو اللي بيتاخد فعليًا

### تقسيم المشروع لـ 2 notebook منفصلين
- `stashface_build.ipynb`: البناء بس (تحميل + كشف + embedding)، بيرفع
  `face_db.npz` على HF في الآخر. محتاج GPU
- `stashface_query.ipynb`: جديد بالكامل. بيحمّل الـ DB الجاهزة من HF
  ويعمل المقارنة بس. **مش محتاج GPU خالص ولا حتى `insightface`/
  `onnxruntime`** - بيستخدم numpy بس، فبيوفر حصة الـGPU بتاعتك
- `dedupe_pipeline.py` بقى بياخد `--mode build` أو `--mode query`.
  الـ imports الثقيلة (`insightface`, `onnxruntime`) بقت lazy جوا
  الدوال اللي فعلاً محتاجاها، مش على مستوى الملف كله - اتأكد إن
  الموديول بيتحمّل بنجاح من غير أي مكتبة منهم متساب أصلًا لما `--mode
  query`

## 2026-09-03 (تحديث رابع - أمان الجلسة)

### مزامنة الـ checkpoints مع HF أول بأول (حماية من فقدان التقدم لو الجلسة راحت خالص)
- المشكلة: `/kaggle/temp` بيتمسح تلقائيًا كل ما جلسة Kaggle تقفل وتفتح واحدة
  جديدة (عمدًا، زي ما موضح في `models/paths.py`) - فلو الجلسة الحالية
  راحت (مش بس الخلية اتقفلت)، كل ساعات البناء المحلية كانت هتضيع رغم
  آلية الـ resumability المحلية
- دلوقتي: بعد كل batch، لو `--hf-dataset-id`/`--hf-token` متوفرين، الـ
  checkpoints (`download_failed.json`, `no_face_detected.json`,
  `multiple_faces_detected.json`, `face_db.npz`) بترفع فورًا لمجلد
  `reports/` على HF مباشرة، مش بس في الآخر
- وقبل ما `main()` يبدأ، `download_checkpoint_from_hf()` بتدور على أي
  checkpoint سابق موجود على HF من محاولة قبل كده وتنزّله محليًا الأول -
  فحتى لو الجلسة كلها اتمسحت وبدأت جلسة جديدة من الصفر، السكريبت هيكمل من
  آخر batch اترفع على HF، مش من صفر
- اتأكد من المنطق ده بموك لـ `huggingface_hub` (حالة فيها checkpoint جزئي
  على HF وحالة مفيش فيها checkpoint خالص)

## 2026-09-03 (تحديث تالت - مهم)

### إصلاح مشكلة رام حرجة في مرحلة الاستعلام
- الكود القديم كان بيبني مصفوفة تشابه كاملة (n × n) في الرام مرة واحدة. على
  حجم البيانات الحقيقي (~127K عنصر) ده معناه ~63 جيجا رام — كان هيكرش بـ
  Out of Memory على Kaggle بعد ما مرحلة البناء (~ساعتين) تخلص
- اتصلحت بحساب التشابه على دفعات (`--query-chunk-size`, افتراضي 2000) -
  كل دفعة بتتقارن مع الـ DB بالكامل، أقرب top-10 بتتاخد منها، وبتترمى من
  الرام قبل الدفعة اللي بعدها. استهلاك الرام بقى محدود (~1 جيجا بدل 63)
- اتأكد إن النتيجة **مطابقة تمامًا** للنسخة القديمة (نفس المجموعات، نفس
  النسب، نفس الـ need_review) على بيانات اختبار فيها تكرارات حقيقية،
  بغض النظر عن حجم الـ chunk

## 2026-09-03 (تحديث تاني)

### تأكيد صريح لاستخدام الـ GPU من عدمه
- كان مفيش أي طريقة تتأكد بيها إن التشغيلة فعليًا شغالة على GPU — لو الـ CUDA runtime فشل يحمّل (زي مشكلة توافق إصدارات كانت موثقة قبل كده في الكود)، كان بيرجع لـ CPU بصمت من غير أي تحذير
- زودت `report_gpu_status()` بتتنفذ أول حاجة في `main()` وبتطبع:
  - هل الـ CUDA runtime متاح فعليًا
  - الـ providers الفعلية اللي الـ AdaFace embedder شغال بيها (GPU ولا CPU)
  - حالة الـ face detection (buffalo_l) - بيستخدم نفس بوابة الـ CUDA
  - تحذير واضح على stderr لو طلبت `--device-id` بس فعليًا بيشتغل CPU

## 2026-09-03

### إعادة هيكلة ناتج الاستعلام: مجموعات بدل أزواج
- شيل `confirmed_duplicates.json` و`needs_human_review.json` نهائيًا
- بدالهم ملف واحد: `duplicate_groups.json`
- كل عنصر فيه مجموعة (`members`) تمثل شخص واحد متكرر عبر أكتر من `id`:
  - كل عضو: `id`, `file_type`, `image`, `cosine_score`, `need_review`
  - الأعضاء متسجلين مرتبين تنازليًا حسب `cosine_score` (الأعلى فوق، الأقل تحت)
  - الـ `cosine_score` بيتحسب بالنسبة لـ "anchor" المجموعة (العضو الأكتر تمثيلاً لباقي الأعضاء، مش أول عضو دخل بالصدفة)
  - `need_review = true` لو `cosine_score < 70`، وإلا `false`
- التجميع بقى بيستخدم Union-Find على top-10 لكل عنصر (مش أقرب نتيجة واحدة بس) — عشان يمسك حالات الشخص المتكرر 3 مرات أو أكتر مش بس زوج بزوج
- الأشخاص اللي مالهمش أي تكرار (مجموعة من عنصر واحد) بيتشالوا من التقرير خالص

### نقل مكان الكود من /kaggle/working لـ /kaggle/temp
- خلية الـ `git clone` في `stashface_kaggle.ipynb` بقت بتحط الكود في `/kaggle/temp/stashface_pipeline` بدل `/kaggle/working/stashface_pipeline`
- كده كل حاجة (الكود + بيانات الموديل + التقارير) بقت تحت `/kaggle/temp`، من غير أي تخزين على `/kaggle/working` خالص

### إنشاء dedupe_pipeline.py + تحديث الـ notebook
- سكريبت جديد بيبني قاعدة بيانات وجوه مؤقتة (في الرام) من ملفي `performers_with_tpdb.json` و`performers_without_tpdb.json` مع بعض، من غير أي اعتماد على `DataManager`/`performers.zvec` القديمين
- بيستخدم نفس الـ detection/embedding القديمين (`buffalo_l` + AdaFace ViT-B)
- تصنيف فشل واضح لكل حالة: `download_failed.json`, `no_face_detected.json`, `multiple_faces_detected.json` — صور فيها أكتر من وش تتعتبر تالفة ومش بتتستخدم
- بناء + استعلام resumable وبـ batching (نفس اتفاقيات المشروع: checkpoint بعد كل batch، سطر ملخص واحد بس، تحميل صور متوازي)
- الاستعلام بيعمل top-10 لكل عنصر، مش top-1 — عشان يقدر يمسك أكتر من نسخة لنفس الشخص
- في الآخر بيرفع كل حاجة أوتوماتيك لمجلد `reports/` جوا نفس الـ HF dataset repo، من غير خلية رفع يدوي منفصلة
- الـ `stashface_kaggle.ipynb` اتعدل يقرا الملفين من نفس الـ dataset، يشغل `dedupe_pipeline.py` بدل `run_kaggle.py`، ويطبع ملخص محلي بسيط في الآخر
